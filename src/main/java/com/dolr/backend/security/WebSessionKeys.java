package com.dolr.backend.security;

/** HTTP session attribute names for the Thymeleaf portal (no Angular / SPA). */
public final class WebSessionKeys {

	public static final String USER_ID = "portalUserId";

	private WebSessionKeys() {
	}
}
